{
  "name": "@cmdctrl/pi",
  "version": "0.1.0",
  "description": "CmdCtrl daemon for the pi coding agent (@mariozechner/pi-coding-agent)",
  "main": "./dist/index.js",
  "bin": {
    "cmdctrl-pi": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "prepublishOnly": "node -e \"const p=require(\\\"./package.json\\\"); const bad=Object.entries(p.dependencies||{}).filter(([,v])=>v.startsWith(\\\"file:\\\")); if(bad.length){console.error(\\\"ERROR: file: dependencies are not allowed in published packages:\\\",bad.map(([k,v])=>k+\\\"=\\\"+v).join(\\\", \\\")); process.exit(1)}\""
  },
  "keywords": [
    "cmdctrl",
    "daemon",
    "pi",
    "coding-agent",
    "ai",
    "agent"
  ],
  "license": "MIT",
  "dependencies": {
    "@cmdctrl/daemon-sdk": "^1.0.6",
    "@mariozechner/pi-coding-agent": "~0.67.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "ts-node": "^10.9.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
